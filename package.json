{
  "name": "twominds",
  "version": "1.0.0",
  "scripts": {
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-3000}",
    "dev": "python -m uvicorn main:app --reload --host 0.0.0.0 --port ${PORT:-3000}"
  },
  "dependencies": {}
}
